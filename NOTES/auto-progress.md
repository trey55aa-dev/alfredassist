# Auto-improvement run log

One dated line per run: journey walked, what was found, what (if anything) shipped.

- 2026-08-02: Walked "returning after several days away" (habits streak break + XP decay/grace, agenda, achievements). Core recovery mechanics (habit "Get Back on Track" list, XP decay-with-refund, "Welcome back" message on Achievements) already work well and match the mission. Found and fixed a real layout bug: on Agenda (and any PageHeader page with many action buttons — the default new-user state with push/calendar integrations unconfigured), the header's action-button row didn't wrap, squeezing the date/title into an unreadable sliver and clipping the last button off-viewport. Fixed in `src/components/PageHeader.tsx` (branch `auto/ux-pass-2026-08-02`, not yet merged).
- 2026-08-02: Picked up the mobile overlap bug noted above instead of walking a fresh journey (it was a known, already-confirmed gap). Reproduced at 375px on the Habits page ("Get Back on Track" cards): `flex-1`+`min-w-0` let the title column shrink to zero width rather than the row wrapping, so "Start fresh today" sat on top of the habit title/badges. Fixed in `src/components/RecoveryPanel.tsx` by switching to `flex-col sm:flex-row`. Verified at 375px and 1280px; `bun run test` (77 passed) and `tsc --noEmit` clean. Branch `auto/recovery-card-mobile-overlap-2026-08-02` (not yet merged as of this run — main was still at `3318399` when this run started).
- 2026-08-04: Walked "goals and progress logging" — seeded 4 goals (annual books target, quarterly debt payoff, a monthly streak goal, and a brand-new zero-progress goal) plus 2 habits. Found the pace/checkpoint math and "Hit X by Sunday" concrete-catchup copy work well and genuinely serve the "make time concrete" principle. But the severely-behind-pace state ("behind_critical") rendered as an all-caps red badge reading **"FALLING APART — N DAYS LATE AT THIS RATE"** with detail text "Cut the target or extend the deadline before this slips further." — alarmist, judgmental language that directly contradicts the documented "grace before penalty" / "work with the user when they fall short, never punish or shame" principles. Fixed in `src/lib/goalsHistory.ts` (`computeProjection`, the `behind_critical` branch): now reads "WELL BEHIND — N DAYS OFF PACE" / "Needs X/day to catch up; current pace is Y/day. Worth adjusting the target or deadline." Added a vitest case in `src/test/goalsHistory.test.ts` asserting the far-behind message never contains "falling apart" / "slips further" / "cut the target", so the tone doesn't regress. Verified in the running app (accessibility tree + full-page screenshot at 1280×2400) on two different behind-pace goals; `bun run test` (78 passed) and `tsc --noEmit` clean. Branch `auto/goals-progress-journey-2026-08-04`.
  - NOT fixed, noted for later: seeding a habit with a missing/undefined `cadence` or `title` field (e.g. from a stray localStorage write, cross-device sync of pre-cadence-era data, or manual tampering) hard-crashes the entire Dashboard with an unrecoverable error screen (`DEFAULT_STEPS_BY_CADENCE[habit.cadence] is not a function` in `src/lib/habits.ts`, `buildRecovery`). `habitsRepo.ts` already defends against this on the cloud-read path (`row.cadence ?? "daily"`), but the local-cache read path in `src/hooks/useCloudHabits.ts` (`readHabitsCache`) does not normalize/validate habits at all before they're used. Worth a small defensive-normalization fix mirroring the cloud path, in the spirit of "nothing is unrecoverable" — deferred this run to keep the single-change budget on the copy/tone fix above, which was the more directly mission-relevant finding.
  - Also noted, not investigated further: `main` was still at `3318399` at the start of this run — the two 2026-08-02 branches above don't appear to have been merged yet, so their fixes (PageHeader button-row wrap, RecoveryPanel mobile overlap) are still only live on their own branches.
- 2026-08-06: Picked up the habit-cadence-crash gap noted on 2026-08-04 instead of walking a fresh journey (it was a known, already-confirmed bug worth fixing before adding more surface area). Reproduced it: seeded `alfred.habits` with a cadence-less habit and a title-less habit with an invalid cadence, confirmed (with the fix temporarily reverted) that the Dashboard falls to the error boundary with `DEFAULT_STEPS_BY_CADENCE[habit.cadence] is not a function`. Added a shared `normalizeHabit()` in `src/lib/habits.ts` (validates `cadence` against the real union, defaults a missing/blank title to "Untitled habit") and used it on both read paths — `habitsRepo.rowToHabit` (cloud, which previously only null-checked cadence, not validated it) and `useCloudHabits.readHabitsCache` (local cache, previously unguarded). Verified in the running app: both malformed habits render normally with full recovery cards, toggling "Start fresh today" on the corrupted one logs and persists correctly across reload, no console errors. Added `src/test/habits.normalize.test.ts` (4 cases). `bun run test` (82 passed) and `tsc --noEmit` clean. Branch `auto/habit-cache-crash-2026-08-06`, PR opened against main.
  - Also noted, not investigated further: `main` was still missing the `auto/ux-pass-2026-08-02` and `auto/recovery-card-mobile-overlap-2026-08-02` fixes as of this run's start (confirmed via `git branch --merged main`) — worth checking whether those PRs are still open or were abandoned.
